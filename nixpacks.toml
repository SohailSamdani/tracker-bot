[phases.setup]
nixPkgs = ["python311"]

[phases.install]
cmds = ["pip install --upgrade pip", "pip install python-telegram-bot==20.7 openpyxl==3.1.2 httpx==0.27.0"]

[start]
cmd = "python bot.py"
```
